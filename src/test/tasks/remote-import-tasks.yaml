includes:
  - remote-more: https://raw.githubusercontent.com/defenseunicorns/maru-runner/${GIT_REVISION}/src/test/tasks/even-more-tasks-to-import.yaml
  - baz: ./more-tasks/baz.yaml
  - private-remote: git::https://gitlab.com/zachariah.miller31/private-task-test.git//tasks.yaml?ref=main
  - private-remote-other-ref: git::https://gitlab.com/zachariah.miller31/private-task-test.git//tasks.yaml?ref=v0.1.0


tasks:
  - name: echo-var
    actions:
      - task: remote-more:set-var
      - cmd: |
          echo "${PRETTY_OK_COMPANY} is a pretty ok company"
      - task: private-remote:all

  - name: local-baz
    actions:
      - task: baz:baz
