tasks:
  - name: default
    description: Run Default Task
    actions:
      - cmd: echo "This is the default task"
      - task: default-do
  - name: default-do
    description: Run Default Task
    actions:
      - cmd: echo "This is the default-do task"
      - task: default
